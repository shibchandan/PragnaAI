{
  "name": "pragna-ai-retrieval-studio",
  "version": "1.0.0",
  "description": "Professional MERN-level implementation of PragnaAI Vector Retrieval Studio",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "powershell -ExecutionPolicy Bypass -File .\\build.ps1",
    "build-all": "npm run build:frontend && npm run build:backend",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -k -p \"[{name}]\" -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
